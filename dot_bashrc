#
# ~/.bashrc
#

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

alias ls='ls --color=auto'
alias grep='grep --color=auto'
PS1='[\u@\h \W]\$ '

#Personnal aliases
alias editconfig='chezmoi edit --watch'

export PATH=$PATH:.local/bin

eval "$(oh-my-posh init bash --config /home/nampoina/.cache/oh-my-posh/themes/amro.omp.json)"
